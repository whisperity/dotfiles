{
  "description": "Installs some common and most wanted system administration and maintenance tools.",
  "superuser": true,
  "prefetch": [
    {
      "kind": "shell",
      "command": "$PACKAGE_DIR/../fd.sh"
    }
  ],
  "install": [
    {
      "kind": "shell multiple",
      "commands": [
        "sudo apt-get install -y --no-install-recommends aptitude cpufreq-utils finger iotop lynx smartmontools traceroute",
        "sudo dpkg --install $PREFETCH_DIR/fd.deb"
      ]
    }
  ]
}
