description: Vim Markdown support
dependencies:
  - vim.pathogen
prepare:
  - action: git clone
    remote: "http://github.com/plasticboy/vim-markdown"
install:
  - action: copy tree
    folder: "$TEMPORARY_DIR/vim-markdown"
    to: "$HOME/.vim/bundle/vim-markdown"
